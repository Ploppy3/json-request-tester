import { DropfileDirective } from './dropfile.directive';

describe('DropfileDirective', () => {
  it('should create an instance', () => {
    const directive = DropfileDirective;
    expect(directive).toBeTruthy();
  });
});
