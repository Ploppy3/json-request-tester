import { SegmentEditorDirective } from './segment-editor.directive';

describe('SegmentEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new SegmentEditorDirective();
    expect(directive).toBeTruthy();
  });
});
